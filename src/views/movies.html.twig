{% block title %}Films
{% endblock %}

{% block body %}
	<br>
	{% for movie in movies %}
		<a href="./movie/{{ movie.id }}">{{ movie.title }}</a>
		<a href="./updateMovie/{{ movie.id }}">Update</a>
		<a href="./deleteMovie/{{ movie.id }}" onclick="return confirm('Are you sure you want to delete this movie?')">Delete</a>
		<br>
	{% endfor %}
	<a href="./addMovie">Add Movie</a>
{% endblock %}
